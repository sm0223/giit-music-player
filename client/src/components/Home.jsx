import React from 'react';
import Header from "./Header";

const Home = () => {
  return (
      <div className="bg-primary w-full h-auto flex flex-col items-center justify-center">
        <Header/>
      </div>
  );
};

export default Home;
